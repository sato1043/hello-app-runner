version: 1.0
runtime: python3
build:
  commands:
    build:
      - yum install -y gcc libffi-devel pycairo
      - pip install pipenv
      - pipenv install -r requirements.txt
    post-build:
      - yum remove -y gcc
run:
  command: pipenv run python app.py
  network:
    port: 8000
